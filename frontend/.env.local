# Local development — Vite proxy handles routing so this isn't needed locally,
# but set it here if you want to hit the backend directly.
# VITE_API_URL=http://localhost:8001

# Google OAuth — paste your Client ID from Google Cloud Console here
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
